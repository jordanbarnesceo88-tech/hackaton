/**
 * Константы доступа к API без зависимостей от Auth.js и БД: их читают и проверка доступа
 * (lib/api/auth.ts), и описание API (lib/api/openapi.ts), которое строится в том числе в
 * unit-тестах.
 */

/** Наименьшая длина токена администратора ADMIN_API_TOKEN: короче — вход по токену выключен. */
export const ADMIN_TOKEN_MIN_LENGTH = 16;
