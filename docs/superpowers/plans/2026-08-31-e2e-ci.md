# Playwright E2E + CI — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Playwright E2E over the full 4-step flow (incl. auth → save → report) plus a ready-to-run GitHub Actions CI workflow (unit + E2E against a Postgres service).

**Architecture:** Additive test infra. `@playwright/test` (Chromium) with a `webServer` that builds+starts the app; three specs in `e2e/`; vitest excludes `e2e/`. A complete `.github/workflows/ci.yml` — inert until a GitHub remote exists (there is none today), green the moment one does. No product-logic changes.

**Tech Stack:** Next.js 16, TypeScript, Prisma 7 (Postgres on host 5433), Vitest, **new: @playwright/test**.

**Spec:** `docs/superpowers/specs/2026-08-31-e2e-ci-design.md`

## Global Constraints

- **Never commit to `master`.** Branch `e2e-ci` (already checked out).
- **DB up:** `docker compose up -d` (host **5433**), seeded (`npm run db:seed`) — the E2E run reads the 3 real warehouse products.
- **Unit tests** run with `npx --yes vitest run < /dev/null` and MUST stay green (106) — adding the `e2e/` exclude must NOT drop or break any unit test.
- **E2E** run with `npm run test:e2e` — must pass locally against the seeded DB as the acceptance check.
- Build `npm run build` exit 0. **No product-logic change** (test infra + config only; at most a `data-testid` if a text anchor is ambiguous — none expected).
- Selectors prefer `getByRole`/`getByText` (Russian UI). Assert real seeded products by **vendor/marker**, never a hardcoded price.

## Confirmed UI text anchors (real, grep-verified)
- Onboarding: radio labels «Торговля», «Склад»; button «Перейти к сравнению решений».
- Compare `/compare/warehouse`: «Рассчитать →» links; «оценка» marker; «открытый источник» (ProvenanceBadge); real vendors: «Hai Robotics», «Exotec», «AutoStore».
- Calculate: hero «Окупается за» / «Не окупается»; «CAPEX»; «Чувствительность NPV»; recommendation «Рекомендация для вашего объекта»; «Сделать основным».
- Signup `/signup`: inputs `name="email"`, `name="password"`, `name="name"`; submit «Зарегистрироваться» (signup redirects to `/` on success).
- Save: «Сохранить расчёт» → «Сохранено» + «Открыть отчёт» link.
- Report `/report/[id]`: «Отчёт ROI»; disclaimer «не оферта».

## File structure
- `package.json` — MODIFY: add `@playwright/test` devDep + `test:e2e` script.
- `playwright.config.ts` — CREATE.
- `vitest.config.mts` — MODIFY: exclude `e2e/**`.
- `e2e/flow.spec.ts`, `e2e/switch.spec.ts`, `e2e/auth-report.spec.ts` — CREATE.
- `.github/workflows/ci.yml` — CREATE.
- `README.md` — MODIFY: E2E + CI note.
- `.gitignore` — MODIFY: ignore `playwright-report/`, `test-results/`, `/playwright/.cache`.

---

### Task 1: Playwright tooling + config + vitest exclude

**Files:**
- Modify: `package.json`, `vitest.config.mts`, `.gitignore`
- Create: `playwright.config.ts`

- [ ] **Step 1: Install Playwright** (devDependency + Chromium binary):

Run: `npm install -D @playwright/test` then `npx playwright install chromium`
Expected: package added; Chromium downloaded.

- [ ] **Step 2: Add the `test:e2e` script** to `package.json` `scripts`:
```json
    "test:e2e": "playwright test",
```

- [ ] **Step 3: Create `playwright.config.ts`**:
```ts
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./e2e",
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: process.env.CI ? [["html", { open: "never" }], ["list"]] : "list",
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run build && npm run start",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 180_000,
  },
});
```

- [ ] **Step 4: Exclude `e2e/` from vitest** — edit `vitest.config.mts`:
```ts
import { defineConfig, configDefaults } from "vitest/config";
```
and inside `test: { … }` add:
```ts
    exclude: [...configDefaults.exclude, "e2e/**"],
```

- [ ] **Step 5: Ignore Playwright artifacts** — append to `.gitignore`:
```
# Playwright
/playwright-report/
/test-results/
/playwright/.cache/
```

- [ ] **Step 6: Verify unit tests unaffected** — `npx --yes vitest run < /dev/null` → **106 passed** (the exclude must not drop any). `npm run build` exit 0.

- [ ] **Step 7: Commit**
```bash
git add package.json package-lock.json playwright.config.ts vitest.config.mts .gitignore
git commit -m "chore: add Playwright (Chromium) + config; exclude e2e/ from vitest"
```

---

### Task 2: The three E2E specs (written + run green locally)

> Acceptance = `npm run test:e2e` passes. Requires the DB up + seeded. The `webServer` builds+starts the app automatically. All three specs navigate the flow to reach a solution rather than hardcoding cuid ids.

**Files:**
- Create: `e2e/flow.spec.ts`, `e2e/switch.spec.ts`, `e2e/auth-report.spec.ts`

- [ ] **Step 1: `e2e/flow.spec.ts`** (anonymous 4-step):
```ts
import { test, expect } from "@playwright/test";

test("anonymous flow: onboarding → compare → calculate", async ({ page }) => {
  await page.goto("/onboarding");
  await page.getByText("Торговля", { exact: true }).click();
  await page.getByText("Склад", { exact: true }).click();
  await page.getByRole("button", { name: "Перейти к сравнению решений" }).click();

  await expect(page).toHaveURL(/\/compare\/warehouse/);
  // Real sourced products + honesty markers.
  await expect(page.getByText("оценка").first()).toBeVisible();
  await expect(page.getByText("открытый источник").first()).toBeVisible();
  await expect(
    page.getByText(/Hai Robotics|Exotec|AutoStore/).first()
  ).toBeVisible();

  await page.getByRole("link", { name: /Рассчитать/ }).first().click();
  await expect(page).toHaveURL(/\/calculate\//);

  // Hero band + results + sensitivity render.
  await expect(page.getByText(/Окупается за|Не окупается/).first()).toBeVisible();
  await expect(page.getByText("CAPEX").first()).toBeVisible();
  await expect(page.getByText("Чувствительность NPV")).toBeVisible();
});
```

- [ ] **Step 2: `e2e/switch.spec.ts`** (in-place recommendation switch):
```ts
import { test, expect } from "@playwright/test";

test("in-place solution switch keeps the URL and updates the page", async ({ page }) => {
  // Warehouse AS/RS has ≥2 real siblings (Exotec + AutoStore).
  await page.goto("/compare/warehouse");
  await page.getByRole("link", { name: /Рассчитать/ }).nth(0).click();
  await expect(page).toHaveURL(/\/calculate\//);

  await expect(page.getByText("Рекомендация для вашего объекта")).toBeVisible();
  const makePrimary = page.getByRole("button", { name: "Сделать основным" }).first();
  // The switch button only appears when there is a non-selected sibling.
  await expect(makePrimary).toBeVisible();

  const heading = page.getByRole("heading", { level: 1 });
  const titleBefore = await heading.innerText();
  const urlBefore = page.url();

  await makePrimary.click();

  // Definitive in-place proof: the h1 (Расчёт экономики: {solution}) changes to the newly
  // selected solution WHILE the URL stays the same (no navigation).
  await expect(heading).not.toHaveText(titleBefore);
  expect(page.url()).toBe(urlBefore);
});
```

- [ ] **Step 3: `e2e/auth-report.spec.ts`** (signup → save → report):
```ts
import { test, expect } from "@playwright/test";

test("signup → save analysis → open the report", async ({ page }) => {
  const email = `e2e+${Date.now()}@example.com`;

  await page.goto("/signup");
  await page.locator('input[name="email"]').fill(email);
  await page.locator('input[name="password"]').fill("password123");
  const nameField = page.locator('input[name="name"]');
  if (await nameField.count()) await nameField.fill("E2E Tester");
  await page.getByRole("button", { name: "Зарегистрироваться" }).click();
  // signUpAction signs in + redirects to "/".
  await expect(page).toHaveURL("http://localhost:3000/");

  // Reach a calculate page via the flow.
  await page.goto("/compare/warehouse");
  await page.getByRole("link", { name: /Рассчитать/ }).first().click();
  await expect(page).toHaveURL(/\/calculate\//);

  await page.getByRole("button", { name: "Сохранить расчёт" }).click();
  await expect(page.getByText("Сохранено")).toBeVisible();

  const reportLink = page.getByRole("link", { name: "Открыть отчёт" });
  await expect(reportLink).toBeVisible();
  await reportLink.click();

  await expect(page).toHaveURL(/\/report\//);
  await expect(page.getByText(/Отчёт ROI/)).toBeVisible();
  await expect(page.getByText(/не оферта/)).toBeVisible();
});
```

- [ ] **Step 4: Run E2E locally — expect PASS** (DB up + seeded first):
```bash
docker compose up -d
npm run db:seed
npm run test:e2e
```
Expected: 3 passed. If a text anchor mismatches, fix the SELECTOR to the real UI text (do not change product code); if a genuinely ambiguous locator needs it, add a minimal `data-testid` to the component and use `getByTestId`.

- [ ] **Step 5: Confirm unit tests still green** — `npx --yes vitest run < /dev/null` (106) — proves the exclude keeps them separate.

- [ ] **Step 6: Commit**
```bash
git add e2e/
git commit -m "test: Playwright E2E over the 4-step flow (flow, switch, auth→save→report)"
```

---

### Task 3: CI workflow + docs

**Files:**
- Create: `.github/workflows/ci.yml`
- Modify: `README.md`

- [ ] **Step 1: Create `.github/workflows/ci.yml`**:
```yaml
# CI: unit + E2E on push/PR. Inert until this repo has a GitHub remote; until then run
# `npm run test:e2e` locally. Runs green the moment a remote exists.
name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: rrp
          POSTGRES_PASSWORD: rrp_dev_password
          POSTGRES_DB: robotization_roi
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U rrp"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://rrp:rrp_dev_password@localhost:5432/robotization_roi
      AUTH_SECRET: ci-throwaway-secret-not-for-production
      CI: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx prisma migrate deploy
      - run: npm run db:seed
      - run: npm run build
      - run: npx vitest run
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
```

- [ ] **Step 2: YAML-validate** the workflow:
```bash
node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); console.log('lines',y.split('\n').length); if(!/jobs:|runs-on:|npm run test:e2e/.test(y)) process.exit(1); console.log('structure ok')"
```
(Or `npx --yes js-yaml .github/workflows/ci.yml` if available.) Expected: parses / structure ok.

- [ ] **Step 3: README note** — add a short "## Tests & CI" section: `npm test` (unit, needs DB up), `npm run test:e2e` (Playwright, needs DB up + seed), and that `.github/workflows/ci.yml` runs automatically **once the repo is pushed to a GitHub remote**.

- [ ] **Step 4: Verify build + unit green** — `npm run build` (exit 0), `npx --yes vitest run < /dev/null` (106).

- [ ] **Step 5: Commit**
```bash
git add .github/workflows/ci.yml README.md
git commit -m "ci: ready-to-run GitHub Actions (postgres + unit + Playwright)"
```

---

## Whole-feature review (after Task 3)
- [ ] `npm run build` (exit 0), `npx --yes vitest run < /dev/null` (106), `npm run test:e2e` (3 passed).
- [ ] `/code-review high` over `git diff master...HEAD`; fix findings.
- [ ] Merge to master with `--no-ff` only after review + user sign-off. Note in the merge that CI activates on adding a GitHub remote.
